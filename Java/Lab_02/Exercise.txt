- Binary Search , consumed time , currentTimeMillis, System.nanoTime

Develop an application that extracts the minimum and
maximum of the elements of an array of 1000 element and
compute the search running time.
• Rewrite the application to implement the binary search
algorithm and compute the search running time.
Hint: Use System.currentTimeMillis() or System.nanoTime ().